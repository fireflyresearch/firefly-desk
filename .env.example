# ---------------------------------------------------------------------------
# Firefly Desk -- Environment Variables
# ---------------------------------------------------------------------------
# Copy this file to .env and fill in the values.
#
#   cp .env.example .env
#
# All FLYDEK_* variables are read by the Python backend via Pydantic Settings.
# The POSTGRES_PASSWORD variable is used by docker-compose for the postgres
# service and should match the password in FLYDEK_DATABASE_URL.
# ---------------------------------------------------------------------------

# -- Database ---------------------------------------------------------------
# Connection string for the async PostgreSQL driver.
# When running via docker-compose, the host is the postgres service name.
FLYDEK_DATABASE_URL=postgresql+asyncpg://flydek:flydek_dev@postgres:5432/flydek

# -- Redis (optional) ------------------------------------------------------
# Used for session state and rate limiting.
# When running via docker-compose, the host is the redis service name.
FLYDEK_REDIS_URL=redis://redis:6379/0

# -- OIDC Authentication ----------------------------------------------------
# Configure these to point to your identity provider (e.g. Keycloak, Auth0).
FLYDEK_OIDC_ISSUER_URL=https://your-keycloak.example.com/realms/your-realm
FLYDEK_OIDC_CLIENT_ID=flydek
FLYDEK_OIDC_CLIENT_SECRET=your-client-secret-here
FLYDEK_OIDC_REDIRECT_URI=http://localhost:3000/auth/callback

# -- Security ---------------------------------------------------------------
# 32-character key used to encrypt stored credentials (e.g. API keys).
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
FLYDEK_CREDENTIAL_ENCRYPTION_KEY=change-me-generate-a-real-key!!

# -- Branding (optional) ---------------------------------------------------
FLYDEK_AGENT_NAME=Firefly Desk Assistant
FLYDEK_APP_TITLE=Firefly Desk
FLYDEK_ACCENT_COLOR=#2563EB

# -- PostgreSQL container ---------------------------------------------------
# Used by docker-compose to set the postgres service password.
# Must match the password in FLYDEK_DATABASE_URL above.
POSTGRES_PASSWORD=flydek_dev
