# ---------------------------------------------------------------------------
# Firefly Desk -- Environment Variables
# ---------------------------------------------------------------------------
# Copy this file to .env and fill in the values.
#
#   cp .env.example .env
#
# All FLYDESK_* variables are read by the Python backend via Pydantic Settings.
# The POSTGRES_PASSWORD variable is used by docker-compose for the postgres
# service and should match the password in FLYDESK_DATABASE_URL.
# ---------------------------------------------------------------------------

# -- Database ---------------------------------------------------------------
# Connection string for the async PostgreSQL driver.
# When running via docker-compose, the host is the postgres service name.
FLYDESK_DATABASE_URL=postgresql+asyncpg://flydesk:flydesk_dev@postgres:5432/flydesk

# -- Redis (optional) ------------------------------------------------------
# Used for session state and rate limiting.
# When running via docker-compose, the host is the redis service name.
FLYDESK_REDIS_URL=redis://redis:6379/0

# -- OIDC Authentication ----------------------------------------------------
# Configure these to point to your identity provider (e.g. Keycloak, Auth0).
FLYDESK_OIDC_ISSUER_URL=https://your-keycloak.example.com/realms/your-realm
FLYDESK_OIDC_CLIENT_ID=flydesk
FLYDESK_OIDC_CLIENT_SECRET=your-client-secret-here
FLYDESK_OIDC_REDIRECT_URI=http://localhost:3000/auth/callback

# -- Security ---------------------------------------------------------------
# 32-character key used to encrypt stored credentials (e.g. API keys).
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
FLYDESK_CREDENTIAL_ENCRYPTION_KEY=change-me-generate-a-real-key!!

# -- Branding (optional) ---------------------------------------------------
FLYDESK_AGENT_NAME=Firefly Desk Assistant
FLYDESK_APP_TITLE=Firefly Desk
FLYDESK_ACCENT_COLOR=#2563EB

# -- PostgreSQL container ---------------------------------------------------
# Used by docker-compose to set the postgres service password.
# Must match the password in FLYDESK_DATABASE_URL above.
POSTGRES_PASSWORD=flydesk_dev
